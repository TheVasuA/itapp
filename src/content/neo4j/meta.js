// Language metadata for Neo4j (Cypher).

/** @type {import('@/lib/content').LanguageMeta} */
const meta = {
  key: 'neo4j',
  label: 'Neo4j Cypher',
  tagline: 'Graph database query language — nodes, relationships, and traversals.',
  description:
    'Learn Neo4j Cypher for creating and querying graph data: nodes, relationships, MATCH, CREATE, pattern matching, and graph traversal.',
  category: 'database-nosql',
  order: 4,
  color: '#008cc1',
  icon: 'Database',
};

export default meta;
