// Language metadata for Redis.

/** @type {import('@/lib/content').LanguageMeta} */
const meta = {
  key: 'redis',
  label: 'Redis',
  tagline: 'In-memory data structure store — blazing-fast caching and messaging.',
  description:
    'Learn Redis data types (strings, hashes, lists, sets, sorted sets), commands, expiration, pub/sub, and caching patterns.',
  category: 'database-nosql',
  order: 2,
  color: '#dc382d',
  icon: 'Database',
};

export default meta;
